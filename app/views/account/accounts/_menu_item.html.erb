<% if can? :read, Account.new(team: current_team) %>
  <%= render 'account/shared/menu/item', {
    url: main_app.polymorphic_path([:account, current_team, :accounts]),
    label: t('accounts.navigation.label'),
  } do |p| %>
    <% p.content_for :icon do %>
      <i class="<%= t('accounts.navigation.icon') %>"></i>
    <% end %>
  <% end %>
<% end %>
