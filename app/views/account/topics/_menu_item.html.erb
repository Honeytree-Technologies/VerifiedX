<% if can? :read, Topic.new(team: current_team) %>
  <%= render 'account/shared/menu/item', {
    url: main_app.polymorphic_path([:account, current_team, :topics]),
    label: t('topics.navigation.label'),
  } do |p| %>
    <% p.content_for :icon do %>
      <i class="<%= t('topics.navigation.icon') %>"></i>
    <% end %>
  <% end %>
<% end %>
