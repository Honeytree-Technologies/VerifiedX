<%= render 'account/shared/menu/item', {
  url: account_settings_path,
  label: t('dashboard.settings.section'),
} do |p| %>
  <% p.content_for :icon do %>
    <i class="<%= t('dashboard.navigation.icon') %>"></i>
  <% end %>
<% end %>
